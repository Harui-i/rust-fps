[package]
name = "fps-web"
version = "0.1.0"
edition = "2024"

[dependencies]
yew = { git = "https://github.com/yewstack/yew/", features = ["csr"] }
fps-core = { path = "../fps-core" }
web-sys = "0.3.82"

